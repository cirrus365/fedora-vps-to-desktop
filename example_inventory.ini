[vps]
# For remote execution against a VPS:
# your_vps_ip ansible_user=root ansible_ssh_private_key_file=~/.ssh/id_rsa

# For local execution (used by install.sh):
# localhost ansible_connection=local
